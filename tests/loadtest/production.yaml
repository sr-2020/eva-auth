tank:
  enabled: true
  package: yandextank.plugins.DataUploader
  token_file: .token

phantom:
  address: auth.evarun.ru
  uris:
  - /api/v1/users
  load_profile:
    load_type: rps
    schedule: const(1, 600s)
console:
  enabled: true
telegraf:
  enabled: false
